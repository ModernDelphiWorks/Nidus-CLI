unit NfeRepository;

interface

uses
  SysUtils,
  NfeInfra,
  NfeInterfaces;

type
  TNfeRepository = class
  private
    FInfra: TNfeInfra;
  public
    constructor Create(const AInfra: TNfeInfra);
    destructor Destroy; override;
    function Find: String;
    function Insert(const AJson: String): String;
    function Update(const AJson: String): String;
    function Delete: String;
  end;

implementation

{ TNfeRepository }

constructor TNfeRepository.Create(const AInfra: TNfeInfra);
begin
  FInfra := AInfra;
end;

destructor TNfeRepository.Destroy;
begin
  FInfra.Free;
  inherited;
end;

function TNfeRepository.Delete: String;
begin
  try
    Result := 'sucesso!';
  except
    raise Exception.Create('falha!');
  end;
end;

function TNfeRepository.Find: String;
begin
  try
    Result := 'sucesso!';
  except
    raise Exception.Create('falha!');
  end;
end;

function TNfeRepository.Insert(const AJson: String): String;
begin
  try
    Result := 'sucesso!';
  except
    raise Exception.Create('falha!');
  end;
end;

function TNfeRepository.Update(const AJson: String): String;
begin
  try
    Result := 'sucesso!';
  except
    raise Exception.Create('falha!');
  end;
end;

end.
